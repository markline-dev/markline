{
  "name": "markline-ai-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Reference Cloudflare Worker that proxies Markline's Ask AI on your provider key — for pure-static docs hosting.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^4.97.0"
  }
}
