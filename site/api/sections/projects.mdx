Project objects represent a documentation site on your account. Create one to
get a build, then deploy it, list your projects, or fetch a single project by
id. Projects are the top-level resource everything else hangs off.

<Note>The slug is derived from the project name and must be unique per account.</Note>
